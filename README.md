# BareMetalIRC
A minimal IRC client built using raw TCP sockets in Python. Implements core IRC protocol commands (NICK, USER, JOIN, PRIVMSG, PING/PONG) with real-time terminal messaging and zero external libraries.
